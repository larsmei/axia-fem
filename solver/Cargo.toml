[package]
name = "axia-fem"
version = "1.8.0"
edition = "2021"
description = "FEM solver with CalculiX INP/FRD compatibility (linear, frequency, buckle, heat, dynamic, NLGEOM, J2, contact, Riks)"
license = "MIT"
authors = ["Lars Meindl"]
repository = "https://github.com/larsmei/axia-fem"
readme = "../README.md"
default-run = "axia"
include = ["src/**/*", "Cargo.toml", "../LICENSE", "../README.md"]

[lib]
name = "axia_fem"
crate-type = ["cdylib", "rlib"]
path = "src/lib.rs"

[[bin]]
name = "axia"
path = "src/bin/axia.rs"
required-features = ["cli"]

[features]
default = ["cli"]
cli = []

[dependencies]
serde_json = "1.0"

[target.'cfg(target_arch = "wasm32")'.dependencies]
wasm-bindgen = "=0.2.128"

# Native sparse backends. PARDISO is loaded at runtime from the library path
# (MKL or Panua). rivrs-sparse is the compiled-in fallback.
[target.'cfg(all(not(target_arch = "wasm32"), target_arch = "x86_64"))'.dependencies]
pardiso-wrapper = { version = "0.1.2", features = ["mkl", "panua"] }

[target.'cfg(all(not(target_arch = "wasm32"), not(target_arch = "x86_64")))'.dependencies]
pardiso-wrapper = { version = "0.1.2", features = ["panua"] }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rivrs-sparse = "0.1.1"
faer = "0.24"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
